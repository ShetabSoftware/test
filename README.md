# Python XO

A two-player command-line XO (tic-tac-toe) game.

## Run

Python 3.10 or newer is required.

```bash
python xo.py
```

Players alternate entering a number from 1 to 9 to place `X` or `O`.

## Test

```bash
python -m unittest
```
